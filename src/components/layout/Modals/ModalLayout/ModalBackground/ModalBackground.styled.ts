import styled from 'styled-components';

export const ModalBackgroundContainer = styled.div`
  position: absolute;
  height: 100%;
  width: 100%;
  background-color: rgba(0, 0, 0, 0.45);
  z-index: 99;

  /* animation: fade 0.125s ease;

  @keyframes fade {
    0% {
      opacity: 0;
    }

    100% {
      opacity: 1;
    }
  } */
`;
